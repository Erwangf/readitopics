package core;

public class Constantes {
	
	/* universal directory separator */
	public static String separateur = System.getProperty("file.separator");
	
	/* maximum number of (recomputed) top documents */
	public static int MAX_COMPUTED_TOPDOCS = 1000;

	// name of the configuration directory (ex : "config" for : ./config/
	public static String CONFIG_DIR = "config";

	// name of the result directory (ex : "result" for : ./results/
	public static String RESULT_DIR = "resultats";

	// name of the input data directory (ex : Constantes.DATA_DIR for : ./data/
	public static String DATA_DIR = "data";

}
